# ── Build artifacts ──
/dist/
build/
*.o
*.a

# ── macOS ──
.DS_Store
*.dmg
*.zip

# ── Secrets / AWS credential backups (never commit) ──
*.bak.*
credentials.bak.*
.env
.env.*
*.pem
*.key
*.p12
*.mobileprovision
*.cer
*.p8
*.ppk
*.jks
*.keystore
*.ovpn
*.ezprofile
.envrc
.direnv/

# Cloud / cluster credentials and local provider state
.aws/
.azure/
.config/gcloud/
.kube/
**/application_default_credentials.json
**/azureProfile.json
**/accessTokens.json
**/credentials
**/kubeconfig
id_rsa
id_ed25519

# IaC state and variable values (lock files remain tracked)
.terraform/
.tofu/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
*.tfplan

# ── Claude Code / ruflo / AI tooling (local dev only — never part of the app) ──
.claude/
.claude-flow/
.claude-flow.db
CLAUDE.md
CLAUDE.local.md
AGENTS.md
.mcp.json
.swarm/
.hive-mind/
.agentdb/
.agentdb*
agentdb.rvf*
.ruv-swarm/
.ruflo/
ruvector.db
memory/
claude-flow*
.roo/
.cursor/
.aider*

# ── Local databases / caches (AI tooling state, never app data) ──
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ── Node / npx ──
node_modules/

# ── Go coverage / analysis output ──
*.out
*.out.tmp
coverage*
cov_*

# Security scanner output can contain matched/redacted source context
gitleaks-report.*
trufflehog-report.*
govulncheck-report.*
grype-report.*
*.sarif
*.log
